import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.tabacarialandpage',
  appName: 'tabacaria-landpage',
  webDir: 'dist'
};

export default config;
