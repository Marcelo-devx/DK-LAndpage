// This hook is intentionally empty.
// The Mercado Pago back_url now points directly to /confirmacao-pedido/:id,
// so no client-side redirect detection is needed.
export const useMercadoPagoRedirect = () => {};
